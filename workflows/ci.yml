# Example workflow for TrustWallet
# name: CI
#
# on:
#   push:
#     branches:
#       - main
#   pull_request:
#     branches:
#       - main

# jobs:
#   build:

#     runs-on: ubuntu-latest

#     steps:
#     - name: Checkout code
#       uses: actions/checkout@v2

#     - name: Set up Go
#       uses: actions/setup-go@v2
#       with:
#         go-version: 1.18

#     - name: Install dependencies
#       run: go mod download

#     - name: Run tests
#       run: go test -v ./...

#     - name: Log in to Docker Hub
#       run: echo "${ secrets.DOCKERHUB_PASSWORD }}" | docker login -u "${ secrets.DOCKERHUB_USERNAME }}" --password-stdin

#     - name: Build Docker image
#       run: docker build -t ricard0/trustwallet:latest .

#     - name: Push Docker image
#       run: docker push ricard0/trustwallet:latest